name: tfm_dbt
version: "1.0.0"
config-version: 2

profile: tfm_dbt

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  tfm_dbt:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
